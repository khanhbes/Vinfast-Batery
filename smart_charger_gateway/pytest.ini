[pytest]
# Keep test scratch data inside the repository. Some managed Windows
# environments deny pytest access to the system temp directory; colocating
# it also makes the gateway test command reproducible in CI and locally.
addopts = --basetemp=.pytest-temp
